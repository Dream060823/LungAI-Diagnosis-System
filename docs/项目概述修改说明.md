# 项目概述.docx 修改说明

以下是需要修改的地方，对照原文改：

---

## 修改1：AI模型部分的描述

### 原文：
> AI模型部分（由林钟鑫负责）
> AI模型是整个系统的大脑。我们的技术路线是"开源预训练模型 + 工程化封装"，而非从零训练。

### 改为：
> AI模型部分（由林钟鑫负责）
> AI模型是整个系统的大脑。我们的技术路线是"开源预训练模型 + 工程化封装"，而非从零训练。

（这段不用改，保持原样）

---

## 修改2：删除U-Net相关内容

### 原文：
> 模型选型：
> 考虑到比赛演示需要在普通笔记本电脑上流畅运行，我们采用2D U-Net作为分割网络。2D U-Net的参数量仅为3D U-Net的三分之一，计算效率高、显存占用低，能在无独立显卡的设备上实现秒级推理。虽然2D方案无法利用CT的层间空间信息，但足以满足比赛场景的演示需求。分类网络采用ResNet-50，对候选结节区域进行良恶性二分类判断。

### 改为：
> 模型选型：
> 采用ResNet-50作为肺结节良恶性分类模型。ResNet-50是微软研究院于2015年提出的深度残差网络，已在ImageNet数据集上完成预训练，具备强大的图像特征提取能力。通过迁移学习方法，在LUNA16肺结节数据集上进行微调，实现结节与非结节的二分类判断。相比3D模型，2D方案参数量小、推理速度快，可在普通笔记本CPU上流畅运行。

---

## 修改3：模型获取策略中的输出描述

### 原文：
> 林钟鑫的核心工作是将下载的预训练模型封装成Python函数接口：输入预处理后的CT切片，输出结节边界框坐标和良恶性概率。

### 改为：
> 林钟鑫的核心工作是将下载的预训练模型封装成Python函数接口：输入预处理后的CT切片，输出良恶性概率，并通过GradCAM热力图技术实现结节区域的自动定位。

---

## 修改4：可解释性输出设计

### 原文（保持不变即可，已经写得很好）：
> 可解释性输出设计：
> 除输出良恶性概率外，系统还设计了对结节影像学特征的提取与展示功能...

### 在原文末尾补充：
> 此外，系统利用CAM热力图的高激活区域实现结节的自动定位。通过对热力图进行阈值处理和轮廓检测，自动提取结节的边界框坐标，标注在原始影像上，帮助医生快速锁定可疑区域。这种方法属于弱监督定位技术，无需额外的检测模型即可实现定位功能。

---

## 修改5：林钟鑫的分工描述

### 原文：
> 林钟鑫：算法工程师
> 整个系统的"大脑提供者"。具体工作包括以下三个方面：
> 第一，从GitHub等开源社区搜索并下载合适的肺结节检测预训练模型，确保模型可以成功运行并输出有效结果。
> 第二，将下载的预训练模型用Python封装成一个标准函数，输入为CT影像图片，输出为结节边界框坐标和良恶性概率，供后端的你进行调用集成。
> 第三，为开发文档中的算法部分提供技术说明文字，包括模型结构简介、输入输出格式说明等。

### 改为：
> 林钟鑫：算法工程师
> 整个系统的"大脑提供者"。具体工作包括以下三个方面：
> 第一，从GitHub等开源社区搜索并下载合适的肺结节分类预训练模型，基于LUNA16数据集进行微调训练，确保模型可以成功运行并输出有效结果。
> 第二，将训练好的模型用Python封装成一个标准函数，输入为CT影像图片，输出为良恶性概率和基于CAM热力图的结节定位边界框，供后端进行调用集成。
> 第三，为开发文档中的算法部分提供技术说明文字，包括模型结构简介、数据预处理流程、CAM热力图原理及结节定位方法等。

---

## 修改6：最终成果中的描述

### 原文：
> 第三步，启动AI分析。评委点击"开始分析"按钮，系统调用后端AI模型进行自动诊断，等待大约三到五秒后分析完成。

### 保持不变，这个描述是准确的。

### 原文：
> 第四步，查看诊断结果。系统自动跳转到结果展示页面。在这个页面上，左侧显示的是原始CT影像，AI检测到的所有结节位置都用醒目的红色矩形框进行了标注；右侧显示的是结构化的诊断报告，内容包括发现了几个结节、每个结节的直径大小、所在肺叶位置、恶性概率百分比，以及综合诊断结论和随访建议。

### 保持不变，现在代码已经能实现这个功能了。

---

## 总结

核心改动就是：把所有提到"2D U-Net"的地方删掉或替换成"ResNet-50分类 + GradCAM定位"。其他内容基本不用动。
