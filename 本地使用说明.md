# LungAI 本地使用说明

## 已完成的配置

- 后端 Python 虚拟环境和依赖：`backend/.venv`
- 前端 Node.js 依赖：`frontend/node_modules`
- 前端生产构建：`frontend/dist`
- 后端病例数据目录：首次启动时自动建立在 `backend/data`

## 最简单的启动方式

双击项目根目录中的 `start-local.bat`。脚本会启动两个服务窗口，并打开：

- 网页界面：http://localhost:5173
- 后端健康检查：http://localhost:5000/health

不要关闭两个服务窗口；关闭它们即停止前端和后端。

## 使用流程

1. 打开网页后，拖入或选择一张 `.dcm` 格式的 DICOM CT 切片。
2. 可填写患者编号和姓名；如果不填，系统会尝试读取 DICOM 元数据。
3. 上传后点击 AI 分析。
4. 在结果区查看结节框、风险概率、诊断结论和随访建议。
5. 历史病例保存在本机 `backend/data/cases`，上传影像保存在 `backend/data/uploads`。

## 当前项目限制

- 目前只支持单张 `.dcm` 切片，不支持整套 DICOM 序列或文件夹。
- 当前后端的 AI 分析是模拟结果（`mock-v0.1`），尚未加载真实 PyTorch 模型；不可用于真实医疗诊断。
- 默认端口为前端 `5173`、后端 `5000`。如果端口已被占用，请先关闭占用程序。

## 手动启动（需要排查问题时）

后端：

```powershell
cd backend
.\.venv\Scripts\python.exe app.py
```

前端（另开一个终端）：

```powershell
cd frontend
npm.cmd run dev
```

Windows PowerShell 如果提示禁止运行 `npm.ps1`，继续使用这里的 `npm.cmd`，无需修改系统执行策略。
